{% extends 'base.html.twig' %}

{% block title %}Universite{% endblock %}

{% block body %}
    <h1>Universite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ universite.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ universite.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ universite.logo }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ universite.created ? universite.created|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Addres</th>
                <td>{{ universite.addres }}</td>
            </tr>
            <tr>
                <th>Classes</th>
                <td>
{#                    {{ dump(universite) }}#}

                 {#   {% if universite.classes|length >0  %}
                {% for class in universite.classes %}
                {{dump(class)}}
                {% endfor %}
                {% endif%}
                #}
                </td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('universite_index') }}">back to list</a>

    <a href="{{ path('universite_edit', {'id': universite.id}) }}">edit</a>

    {{ include('universite/_delete_form.html.twig') }}
{% endblock %}
